const express = require('express');
const router = express.Router();

// Placeholder
router.get('/', (req, res) => res.json([]));
router.get('/vehiculo-completo', (req, res) => res.json([]));

module.exports = router;